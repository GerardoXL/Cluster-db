global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres-exporter:9187']

# --- cada 10 segundos ve la direccion postgres-exporter:9187 y descarga todas las metricas de postgresql---
