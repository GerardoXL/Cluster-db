# --- aumentar el limite de clientes ----
max_connections=300 
# ----
listen_addresses = '*'
wal_level = replica
max_wal_senders = 5
max_replication_slots = 5
hot_standby = on
timezone = 'UTC'
hba_file = '/etc/postgresql/pg_hba.conf'